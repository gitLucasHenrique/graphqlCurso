type Query {
  ola: String!
  teste: String!
  horaCerta: String!
  horaDate: Date!
  usuarioLogado: Usuario
  produtoEmDestaque: Produto
  numerosMegaSena: [Int!]!
  usuarios: [Usuario]
  usuario(id: Int): Usuario
  usuarioDes(id: Int): Usuario
  perfis: [Perfil]
  perfil(id: Int): Perfil
}
